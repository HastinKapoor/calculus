{}

P0(spinlock_t *lo, int *x){
spin_lock(lo);
	smp_mb__after_spinlock();
	Write(x, 1, Relaxed, Linux, None);
	spin_unlock(lo);
}

P1(spinlock_t *lo, int *x){

	Read(x, None, Acquire, Linux, r1);
	r2 = spin_is_locked(lo);
	r3 = spin_is_locked(lo);
}

exists (r1=1 /\ r2=0 /\ r3=1)
