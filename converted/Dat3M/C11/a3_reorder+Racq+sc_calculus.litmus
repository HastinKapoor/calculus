{ [x] = 0; [y] = 0; [zero] = 0; }

P0(x, y) {
	Write(y, 1, Relaxed, C, None);
	Write(x, 1, Release, C, None);
}

P1(x, y, zero) {
	Read(y, None, Acquire, C, r0);
	Read(x, None, Seq_cst, C, r1);
}

exists (r1=1)
