P0(x, y) {
	Write(x, 1, Relaxed, Linux, None);
	Write(y, 1, Release, Linux, None);
}

P1(y, z) {
	Read(y, None, Acquire, Linux, r0);
	Write(z, 1, Release, Linux, None);
}

P2(x, z) {
	Read(z, None, Acquire, Linux, r1);
	Read(x, None, Relaxed, Linux, r2);
}

exists (r0=1 /\ r1=1 /\ r2=0)
