C RCU_test_3

{}

P0(int *x, int *y)
{
	int r0 = READ_ONCE(*x);
	smp_store_release(y, 1);
	synchronize_rcu();
}

P1(int *x, int *y)
{
	rcu_read_lock();
	int r0 = READ_ONCE(*y);
	WRITE_ONCE(*x, 1);
	rcu_read_unlock();
}

exists (0:r0=1 /\ 1:r0=1)
