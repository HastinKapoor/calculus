C release_acquire_read_write_control_dependency

{}

P0(int *x, int *y)
{	
	int r0;
	
	r0 = READ_ONCE(*x);
	smp_store_release(y, 1);
}

P1(int *x, int *y)
{
	int r0;

	r0 = smp_load_acquire(y);
	if(r0 == 1){
		WRITE_ONCE(*x, 1);
//	} else {
//		WRITE_ONCE(*x, 1);
	}
}

exists (0:r0=1 /\ 1:r0=1)
