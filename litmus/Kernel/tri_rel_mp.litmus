C triple_release_message_passing

{}

P0(int *x, int *y)
{
	smp_store_release(x, 1);
	smp_store_release(y, 1);
}

P1(int *x, int *y)
{
	int r0;
	int r1;

	smp_store_release(y, 2);
	r0 = READ_ONCE(*y);
	r1 = READ_ONCE(*x);
}

exists (1:r0=2 /\ 1:r1=0) 
