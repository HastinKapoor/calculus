C read_write_write_control_dependency

{}

P0(int *x, int *y)
{	
	int r0;
	
	r0 = READ_ONCE(*x);
	smp_mb();
	WRITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int r0;

	r0 = READ_ONCE(*y);
	if(r0 == 1){
		WRITE_ONCE(*x, 1);
	} else {
		WRITE_ONCE(*x, 1);
	}
}

exists (0:r0=1 /\ 1:r0=1)
